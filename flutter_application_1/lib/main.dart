import 'package:flutter/material.dart';

void main() {
  runApp(new MaterialApp(
      title: "Tótec",
      home: new Scaffold(
        appBar: new AppBar(
          title: new Text("Tótec"),
        ),
        body: new Container(child: new Center(child: new Text("Los tkm"))),
      )));
}
